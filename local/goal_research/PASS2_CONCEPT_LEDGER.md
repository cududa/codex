# Pass 2 Concept Ledger

This is a Pass 2A coverage artifact. It is not future implementation authority
and does not close any source row.

Use this file to track cross-cutting Goal concepts before rewriting source
docs into successor docs. Repetition in the source docs is recorded here as
coverage, not as a requirement to preserve duplicate prose mechanically.

Repeated authority should be compressed through
`SUCCESSOR_DOC_COMPRESSION_GUIDE.md`: canonical text in the owner target, local
reminders where a seam can violate the rule, pointer-only references where
another target owns the rule, and operational/test reminders where appropriate.
That compression still requires source coverage checks when a disputed concept,
suspected loss, or conflict is named.

Target homes use the provisional interface keys defined in
`PASS2_SECTION_TRACEABILITY.md`. This ledger should not give a key a second
meaning; if a concept only needs a pointer from another interface, keep that
owner/shared/pointer-only routing consistent with the compression guide rather
than broadening ownership here.

## Status Key

| Status | Meaning |
| --- | --- |
| `Open` | Inventoried but not rewritten. |
| `High-risk` | Easy to weaken or accidentally invert during synthesis. |
| `Cross-cutting` | Must be checked across multiple target docs. |
| `Test-critical` | Replacement tests must prove the concept directly. |
| `Code-grounding debt` | Source docs require this concept, but concrete implementation semantics must be researched before target docs treat it as implementable. |
| `Design-pass required` | Code terrain has been researched enough to show existing code does not answer the concept; a successor design pass must choose the implementation shape before execution planning. |

Debt markers are not permission to keep settled behavior ambiguous. Before
successor-doc compression treats a row as unresolved, classify it:

- Rows settled by the source corpus should be rewritten deterministically, even
  if they remain high-risk fidelity guardrails.
- For direct implementation work, implementation details clarified outside
  `local/goal_research` should be represented in the applicable authority docs
  before an implementation plan relies on them.
- For successor-doc compression, relevant `local/goal_136_plan/work-areas`
  decisions are reconciliation inputs only when a disputed concept, suspected
  loss, or conflict is named. If they are more precise than, or appear to
  conflict with, older source-doc wording, use the work-area route when it
  preserves the underlying concept and latest researched v136 design, then
  represent that reconciliation in the owning successor doc.
- Target-home ownership questions should name owner/shared/pointer-only
  routing without changing behavior.
- Direct implementation plan/authority conflicts require fixing the source
  contract or the plan explicitly. Source/work-area conflicts in future rewrite
  planning should be reconciled under the rule above unless the work-area route
  would drop, invert, or weaken a core concept.
- True design gaps require design debt, not authoritative prose pretending the
  answer exists.

## Concept Coverage

| Concept | Canonical owner | Source coverage | Clauses and edge cases to preserve | Code/test anchors | Target homes | Status |
| --- | --- | --- | --- | --- | --- | --- |
| Goal authority | `T-BEHAVIOR` | Grounding Truth: Core Truth, Required Active Steering Shape; Primary Cadence: Non-Negotiable Shape, Current Authority; Final Input: Core Rule | Authority exists only when current Goal steering is in final model request input as outer developer-role model input. Durable state, UI/projection hiding, helper output, source tags, rendered markers, raw events, and tool output are insufficient by themselves. | `Prompt.input`, `ResponsesApiRequest.input`, final payload tests | `T-BEHAVIOR`, `T-FINAL` | High-risk, Cross-cutting, Test-critical |
| Developer-role active steering | `T-BEHAVIOR` | Grounding Truth: Core Truth, Required Active Steering Shape, Anti-Patterns/User-Role Goal Steering; Primary Cadence: Non-Negotiable Shape; Fake-Shim Map: active target | Active Goal steering must be one outer `ResponseItem::Message` with role `developer`. User-role active steering has no compatibility exception. Adding a helper or role enum is not enough without final-input proof. | `ResponseItem::Message.role`, config `steering_role`, final request payload tests | `T-BEHAVIOR`, `T-FINAL`, `T-EXT` | High-risk, Test-critical |
| Internal-context provenance | `T-CLEANUP` | Grounding Truth: Core Truth, Required Active Steering Shape; Primary Cadence: Shared Classification; Fake-Shim Map: What To Replace With; Repair/Classifier: Classifier Outputs | Source-tagged internal context may render Goal text and support classification/projection cleanup. It is provenance and cleanup support, not authority. A user-role internal context remains user-role model input. | Generic internal context helpers, current `ContextualUserFragment` terrain | `T-CLEANUP`, `T-FINAL`, `T-BEHAVIOR` | High-risk, Cross-cutting |
| Active Goal steering text shape | `T-FINAL` | Grounding Truth: Core Truth; Primary Cadence: Non-Negotiable Shape; Final Input: Core Rule; Fake-Shim Map: What To Replace With | Text should be rendered current Goal internal context with `source = "goal"` and objective escaped as untrusted text. Exact Rust names may vary, but role/provenance/body/objective responsibilities cannot. | Goal prompt rendering, final request input tests | `T-FINAL`, `T-BEHAVIOR`, `T-CLEANUP` | Open, Test-critical |
| Final model request input | `T-FINAL` | Grounding Truth: Terminology, Required Active Steering Shape; Primary Cadence: Final Model Request Input; Final Input: Code Terrain, Core Rule | The relevant input is the logical `Vec<ResponseItem>` that becomes `Prompt.input` and `ResponsesApiRequest.input`, before transport-specific deltas. Rendering, constructing, reserving, carrying, or same-turn cadence delivery/recheck requests are not final input. | `codex-rs/core/src/session/turn.rs`, `client_common.rs`, `client.rs`, `codex-api/src/common.rs` | `T-FINAL`, `T-BEHAVIOR` | High-risk, Test-critical |
| Final request-input shaping | `T-FINAL` | Final Input: Final Request-Input Shaping, Shaping Responsibilities; Primary Cadence: Final Model Request Input; Repair/Classifier: Final Request-Input Repair; Model Visible History Key: Capture Point | Runs per attempt after base input is known. Inspects actual input, classifies Goal-looking items, computes or receives the model-visible history key at the shaping capture point, removes/replaces stale/wrong-role/duplicate/legacy/pre-injected items, selects at most one due Goal item, renders from durable facts, inserts/verifies developer role, returns commit evidence. Must not insert merely because active durable Goal exists. | `run_sampling_request`, `build_prompt`, final payload tests, history key capture | `T-FINAL`, `T-CLEANUP`, `T-CADENCE`, `T-HISTORY` | High-risk, Test-critical |
| Commit point | `T-FINAL` | Grounding Truth: Terminology/Primary Cadence; Primary Cadence: Cadence Is Primary, Final Model Request Input; Final Input: Commit Point | Commit occurs only once request is known to have entered model execution, expected `ResponseEvent::Created` unless code walk finds a better point. Before side effects, commit verifies the finalized request-input fingerprint, selected item index, and item fingerprint. No commit on render, item construction, shaping error, prompt build not submitted, stream setup failure, or helper output. | `ResponseEvent::Created`, stream setup paths, finalized-input fingerprint checks | `T-FINAL`, `T-DURABLE`, `T-HISTORY` | High-risk, Test-critical |
| Recorded request evidence | `T-EVIDENCE` | Grounding Truth: Acceptance Standard, Conformance Requirements; Primary Cadence: Final Model Request Input, Verification Checklist, Version Plan Requirements; Idle: Stage 2, Stage 3; Model Visible History Key: Resume And Restart; Final Input: Recorded Request Evidence; Recorded Evidence: entire doc | Final model payload remains primary live verification evidence. Any recorded replay or audit evidence must be structured recorded request evidence that represents the same logical final model request input. Existing ordinary rollout `ResponseItem` replay and optional rollout trace are not sufficient durable Goal request evidence. Recorded request evidence is structured committed Goal request metadata for replay, audit, and reconstruction support. It is not Goal authority, cadence selection, pending intent storage, final-input inspection, or active Goal recovery. Live correctness remains owned by durable state unless rollout-derived evidence is persisted through a non-best-effort path with an equivalent error policy. | `RolloutItem::ResponseItem`, `RolloutItem::GoalRequestEvidence`, rollout reconstruction, thread-history replay, optional rollout trace, final payload/evidence tests | `T-EVIDENCE`, `T-DURABLE`, `T-FINAL`, `T-HISTORY`, `T-CLEANUP`, `T-TEST-PREP`, `T-READINESS` | High-risk, Cross-cutting, Test-critical |
| Commit metadata and item fingerprint | `T-FINAL` | Final Input: Commit Metadata, Commit Point, Current-Turn Carry; Recorded Evidence: Evidence Shape, Fingerprints; Model Visible History Key: Resume And Restart | Commit evidence must tie thread, turn, goal, kind, facts version, history key, exact item fingerprint, and inserted/verified status to the item actually sent. Fingerprint can be structured but must prove exact item identity. When structured evidence is needed, the finalization/commit seam must also expose stable finalized-input identity used by `T-EVIDENCE`, including attempt ordinal, request-input fingerprint, and selected item index. `T-EVIDENCE` owns persistence and replay semantics, not final-input selection. | Commit tests, carry metadata, finalized-input fingerprint helpers, optional watermark storage | `T-FINAL`, `T-HISTORY`, `T-EVIDENCE` | High-risk, Test-critical |
| Durable Goal facts | `T-DURABLE` | Grounding Truth: Durable State; Primary Cadence: Durable Goal Facts, Current Authority; Durable State: Durable Ownership, Storage Shape | Persisted source of current Goal facts: identity, objective, status, budget, usage, timestamps. Current Goal rendering reads durable state, not tool/app-server request bodies, UI, historical rendered text, or rollout artifacts. | `thread_goals`, `thread_goal.rs`, runtime goals | `T-DURABLE`, `T-CADENCE` | Open |
| Durable facts version | `T-DURABLE` | Primary Cadence: Pending Cadence Intent, Runtime Continuation Accounting; Durable State: Code Terrain, Storage Shape, Mutation Rules; History Key: Runtime Watermark | Cadence needs a durable monotonic facts identity that changes when steering-relevant Goal facts change. Current `updated_at_ms` is product metadata and should not be the only cadence facts identity. | State migration/model/runtime | `T-DURABLE`, `T-HISTORY`, `T-CADENCE` | High-risk |
| Pending cadence intent | `T-DURABLE` | Grounding Truth: Primary Cadence; Primary Cadence: Pending Cadence Intent; Idle: Stage 2; Durable State: Storage Shape, Mutation Rules; Final Input: Commit Point | Initial, ObjectiveUpdated, and BudgetLimit persist structured pending intent until final input contains the matching developer-role item. Intent is not rollout text, rendered context, UI metadata, raw events, or helper output. Continuation is excluded. Idle delivery additionally requires feature/mode eligibility, current thread/goal match, status eligibility, facts-version match or intentional supersedence, and no active turn or pending non-Goal work appearing during the check. Selection, rendering, construction, reservation, same-turn cadence recheck/request metadata, idle-hook firing, or ineligible final shaping do not consume intent. Construction failure or built-not-submitted without structured recorded request evidence with an equivalent persistence and error policy leaves intent pending. | Pending intent table/store APIs, Created-event commit path | `T-DURABLE`, `T-CADENCE`, `T-IDLE`, `T-FINAL` | High-risk, Test-critical |
| Exact-key consumption | `T-DURABLE` | Durable State: Required Store Operations; Primary Cadence: Final Model Request Input; Final Input: Commit Point | Consuming pending intent requires matching thread, goal, kind, and facts version, or an explicit supersedence rule. It must not consume a newer Goal, different kind, or different facts version. | `consume_pending_intent_exact`, state tests | `T-DURABLE`, `T-FINAL` | High-risk, Test-critical |
| Cadence events | `T-CADENCE` | Grounding Truth: Primary Cadence; Primary Cadence: Steering Kinds; Idle: Stage 2/3 | Cadence events are Initial, ObjectiveUpdated, BudgetLimit, and automatic Continuation. Only cadence decides when Goal should speak; repair does not. | Cadence state/final input/idle tests | `T-CADENCE`, `T-IDLE`, `T-FINAL` | Open, Cross-cutting |
| Supersedence | `T-CADENCE` | Primary Cadence: Supersedence Rules, Steering Kinds; Durable State: Supersedence; Idle: Stage 2 | For one final request opportunity choose BudgetLimit > ObjectiveUpdated > Initial > Continuation. Continuation never supersedes persisted pending intent. State may perform mechanical cleanup but does not choose eligible request intent. | State cleanup and final shaping tests | `T-CADENCE`, `T-DURABLE`, `T-FINAL`, `T-IDLE` | High-risk |
| Initial steering | `T-CADENCE` | Primary Cadence: Initial; Grounding Truth: Primary Cadence; Idle: Stage 2, Resume Behavior | Initial is due when a newly active Goal enters the active run. Persist Goal facts and pending Initial before delivery. Ordinary turns may deliver pre-existing pending Initial. Resume must not fabricate Initial merely because an active Goal exists. | Create/replace Goal, resume tests | `T-CADENCE`, `T-DURABLE`, `T-IDLE`, `T-FINAL` | High-risk, Test-critical |
| ObjectiveUpdated steering | `T-CADENCE` | Primary Cadence: ObjectiveUpdated; Idle: External Goal Mutation Behavior; Durable State: Mutation Rules; Ext Ownership: Required Replacement Shape | Persist updated objective and pending ObjectiveUpdated first; render from durable state, not request body; include one developer-role item in final request input; consume only when final input contains the matching item. If same-turn cadence recheck is unavailable or rejected, pending intent remains. ObjectiveUpdated supersedes Initial/Continuation unless BudgetLimit supersedes it. | External mutation, state/final input tests | `T-CADENCE`, `T-DURABLE`, `T-IDLE`, `T-EXT`, `T-FINAL` | High-risk, Test-critical |
| BudgetLimit steering | `T-CADENCE` | Primary Cadence: BudgetLimit; Durable State: Mutation Rules/Supersedence; Idle: External Goal Mutation Behavior; Test Deletion Map: baseline facts | Account usage/status first, persist pending BudgetLimit, render current durable objective and budget status, include one developer-role item in final request input, and consume only at final input commit. If same-turn cadence recheck is unavailable or rejected, pending intent remains. BudgetLimit supersedes Initial, ObjectiveUpdated, and Continuation for the same opportunity. Budget and usage are upstream Goal facts. | Usage accounting, budget/status/final input tests | `T-CADENCE`, `T-DURABLE`, `T-IDLE`, `T-TEST-PREP`, `T-FINAL` | High-risk, Test-critical |
| Automatic Continuation | `T-IDLE` | Grounding Truth: Primary Cadence; Primary Cadence: Continuation, Runtime Continuation Accounting; Idle: Stage 3; History Key docs | Continuation is only selected by idle lifecycle after pending work and pending durable intent are absent. It requires Goals feature and collaboration-mode eligibility. It is not any next request, not active Goal existence, not a user turn, and not pending non-Continuation delivery. | `MaybeContinueIfIdle`, history key, final payload tests | `T-IDLE`, `T-HISTORY`, `T-CADENCE`, `T-FINAL` | High-risk, Test-critical |
| Feature and collaboration eligibility | `T-IDLE` | Final Input: Final Request-Input Shaping; Idle: Stage 2, Stage 3; Grounding Truth and Primary Cadence idle predicates | Final shaping context and idle eligibility must carry feature and collaboration-mode facts. Pending intent delivery and automatic Continuation require the Goals feature and a collaboration mode that allows Goal steering. Final request-input shaping treats ineligible attempts as selecting no active Goal item and consuming no pending intent. Eligibility facts are delivery gates, not cadence authority, durable Goal facts, helper authority, or proof that Goal steering reached final model request input. | final shaping attempt context, idle eligibility checks | `T-IDLE`, `T-CADENCE`, `T-FINAL` | High-risk, Cross-cutting |
| Ordinary user turns | `T-CADENCE` | Grounding Truth: Ordinary User Turns; Primary Cadence: Ordinary User Turns; Idle: Legal Callers, Stage 1 | Ordinary user turns are not cadence events and must not receive fresh full Goal Continuation merely because a Goal is active. They may deliver already-pending Initial/ObjectiveUpdated/BudgetLimit or receive repair for cadence-required seam loss. | User-turn final input tests | `T-CADENCE`, `T-IDLE`, `T-FINAL` | High-risk, Test-critical |
| Cadence-required authority | `T-CADENCE` | Grounding Truth: Terminology; Primary Cadence: Cadence Is Primary; Repair tables | Narrowly means pending non-Continuation intent, automatic Continuation selected by idle predicate, or seam preservation/repair of already-required cadence item. Active durable Goal state alone is not cadence-required authority. | Final shaping/repair tests | `T-CADENCE`, `T-CLEANUP`, `T-FINAL` | High-risk |
| Request repair | `T-CLEANUP` | Grounding Truth: Request Repair and Repair Decision Table; Primary Cadence: Request Repair; Repair/Classifier: Final Request-Input Repair; Idle: Request Repair Interaction | Repair is request-local backstop for seam loss/staleness/duplication/wrong role. It is not cadence, cannot turn active state alone into steering, and records only for cadence consumption or structured reconstruction of a lost recorded cadence item. In idle-created requests, repair cannot create pending intent, consume pending intent unless explicitly delivering that pending intent, or advance the Continuation watermark. | Compaction/resume/rollback/fork/retry/reconstruction/model-context seams | `T-CLEANUP`, `T-FINAL`, `T-CADENCE`, `T-IDLE` | High-risk, Test-critical |
| Current authority proof sources | `T-FINAL` | Primary Cadence: Proving Current Authority; Grounding Truth: Request Repair; Final Input: Current-Turn Carry | Valid proof requires final input inspection/accounting. Valid examples include final `for_prompt` output, known same-turn WebSocket reuse, committed current-turn carry. Invalid sources include durable state alone, UI projections, raw events, legacy artifacts, helper output, hidden classification, rendered parsing, and cross-turn previous_response_id alone. | `ContextManager::for_prompt`, previous_response_id, WebSocket delta | `T-FINAL`, `T-CLEANUP` | High-risk, Cross-cutting |
| Retry behavior | `T-FINAL` | Grounding Truth: Terminology, Acceptance Standard; Primary Cadence: Final Model Request Input; Final Input: Retry And Follow-Up | Shaping must run for every model request attempt. Retry before commit leaves pending intent and Continuation watermark unchanged. If final input is built but not submitted, pending intent remains and watermark does not advance by default; any claim that structured recorded request evidence proves otherwise must satisfy the T-EVIDENCE non-best-effort persistence and error-policy boundary and cannot rely on ordinary rollout items or trace. Submission failure before Created defaults to pending unless an explicit tested retry policy says otherwise. Retry after commit reruns against committed state/history. Uncommitted cadence request metadata may survive retries before Created, but Created commit clears or obsoletes it. T-IDLE is pointer-only for Goal-owned synthetic request metadata lifecycle, stale abort-before-submit, and reservation clearing; retry orchestration stays with T-FINAL. | `run_sampling_request` retry loop | `T-FINAL`, `T-DURABLE`, `T-HISTORY`, `T-EVIDENCE`, `T-IDLE` | High-risk, Test-critical |
| Same-turn follow-up | `T-FINAL` | Final Input: Retry And Follow-Up; Primary Cadence: Verification Checklist; Grounding Truth: Acceptance Standard | Follow-up after tool output or mailbox input reruns shaping from rebuilt prompt input. After Created, follow-up attempts assemble fresh context from durable Goal state, pending-intent or Continuation watermark state, optional new turn request metadata, and committed carry; they must not reuse stale pre-commit request metadata as if the original cadence request were still pending. Current-turn carry may preserve already-committed evidence but cannot create authority for a different attempt. T-IDLE is pointer-only when the stale metadata came from a Goal-owned synthetic request. | Tool follow-up tests, mailbox paths, Continuation watermark read-after-commit | `T-FINAL`, `T-CADENCE`, `T-HISTORY`, `T-IDLE` | High-risk, Test-critical |
| Current-turn carry | `T-FINAL` | Primary Cadence: Current-Turn Carry; Final Input: Current-Turn Carry; Repair/Classifier: Compaction; Fake-Shim Map: Compaction/Active Core Steering | Carry is turn-local committed metadata for a Goal item already included in final input. It must not store pre-finalizer concrete `ResponseInputItem`s as authority, create cadence intent, or prove different request attempts. | `codex-rs/core/src/state/turn.rs`, compaction paths | `T-FINAL`, `T-CLEANUP` | High-risk, Test-critical |
| Model-visible history key | `T-HISTORY` | Primary Cadence: Runtime Continuation Accounting; Idle: Stage 3, Resume Behavior; Model Visible History Key: entire doc | Key answers whether model-visible non-Goal progress changed enough to justify another Continuation for the same Goal/facts. `ContextManager::history_version()` is not sufficient by itself. | `history.rs`, `session/mod.rs`, `turn.rs`, `rollout_reconstruction.rs` | `T-HISTORY`, `T-IDLE` | High-risk, Test-critical |
| Eligible progress projection | `T-HISTORY` | Model Visible History Key: Eligible Progress Projection, Capture Point, Compaction And Reconstruction; Repair/Classifier: Compaction/Reconstruction | Include model-visible user/assistant/reasoning/tool/shell/web-search/image-generation/mailbox/hook-prompt/compaction progress. Exclude automatic Continuation item being considered, pure current Goal, legacy artifacts, stale/wrong/pre-injected Goal items, non-progress contextual fragments, raw/UI counts, helper output. | History key tests | `T-HISTORY`, `T-CLEANUP`, `T-FINAL` | High-risk, Test-critical |
| Continuation watermark | `T-HISTORY` | Primary Cadence: Runtime Continuation Accounting; Idle: Stage 3; Model Visible History Key: Runtime Watermark, Resume And Restart; Final Input: Commit Point; Recorded Evidence: Resume And Continuation Suppression | Runtime duplicate suppression compares goal id, model-visible history key, and durable facts version. It advances only after the Continuation item appears in final input as an outer developer-role Goal item and the request reaches commit point. It must not advance on helper output, wrong-role/pre-finalizer construction, reservation, build-not-submitted, or pre-Created failure. The comparison triple is distinct from stored/reconstructed committed suppression evidence, which also needs committed turn, item fingerprint, and timestamp identity. Resume precedence is durable watermark or equivalent state-owned record first, explicitly supported non-best-effort structured evidence second, otherwise no reconstructed watermark. | Optional watermark table, committed suppression record, structured evidence metadata | `T-HISTORY`, `T-IDLE`, `T-FINAL`, `T-DURABLE`, `T-EVIDENCE` | High-risk, Test-critical |
| Resume hydration | `T-IDLE` | Grounding Truth: Primary Cadence/Acceptance Standard; Primary Cadence: Ordering With Pending Work; Idle: Resume Behavior; History Key: Resume And Restart | Resume reloads durable facts, pending intent, accounting basis, and Continuation suppression basis. It must not create Initial, infer intent from artifacts, emit steering, consume intent, or advance watermark. Already-pending Initial remains due; already-consumed Initial is not re-emitted. | `GoalRuntimeEvent::ThreadResumed`, app-server resume ordering | `T-IDLE`, `T-HISTORY`, `T-DURABLE` | High-risk, Test-critical |
| Idle legal callers and reentrancy | `T-IDLE` | Idle: Legal Callers; Idle: Required Stage Order; Idle: Lock And Reservation | The idle hook may run only after ordered idle opportunities such as turn finish, abort/interrupt active-turn clear, resume response/snapshot/replay completion, external mutation persistence, or explicit ordered APIs. It is not ordinary user prompt assembly. Multiple or racing calls must remain idempotent through active-turn checks, lock/reservation behavior, pending-work checks, final-input construction, and Continuation watermarking. | App-server/thread API hooks, idle lock/reservation tests | `T-IDLE` | High-risk, Test-critical |
| Idle hook stage order | `T-IDLE` | Idle: Purpose, Required Stage Order, Stages 1-3; Primary Cadence: Ordering With Pending Work | Logical order is active-turn check, pending non-Goal work, lock/recheck, pending durable cadence intent, recheck, automatic Continuation. One hook invocation returns after starting a turn. | `maybe_continue_goal_if_idle_runtime`, input queue | `T-IDLE` | High-risk, Test-critical |
| Pending non-Goal work precedence | `T-IDLE` | Idle: Stage 1; Primary Cadence: Ordering With Pending Work; Test profile | Queued response items, trigger-turn mailbox input, and future regular pending work outrank Goal-owned synthetic turns. Regular pending-work turn may consume pending intent, but the pending work itself is not a Continuation trigger. | `input_queue.rs`, mailbox/queued tests | `T-IDLE`, `T-CADENCE` | High-risk, Test-critical |
| Goal-owned synthetic turns | `T-IDLE` | Idle: Semantic Contract, Stage 2, Stage 3, Lock And Reservation, Acceptance Tests | The idle hook may start synthetic regular cadence-delivery turns for pending intent or automatic Continuation. Pending Initial/ObjectiveUpdated/BudgetLimit delivery from the hook is not automatic Continuation. Synthetic request metadata is uncommitted scheduling metadata until stale abort clears it or Created commit records carry and clears/obsoletes it. Final payload tests must still prove the synthetic turn produces exactly one developer-role Goal item when cadence is delivered and no active `<goal_context>` items. | Idle hook/final payload tests | `T-IDLE`, `T-CADENCE`, `T-FINAL`, `T-TEST-PREP` | High-risk, Test-critical |
| Lock, reservation, and stale candidates | `T-IDLE` | Idle: Legal Callers, Required Stage Order, Lock And Reservation, Acceptance Tests | Lock/reservation protects scheduling but does not make stale candidates valid. Recheck active turn, pending work, durable state, pending intent, and watermark. If pending work appears or candidate stales, clear reservation without send, consumption, watermark, or user-facing model/request error. Goal-owned launch must not drain newly arrived pending non-Goal work into the synthetic turn; that work remains regular pending work. | `continuation_lock`, `ActiveTurn` | `T-IDLE`, `T-FINAL` | High-risk, Test-critical |
| External Goal mutation ordering | `T-IDLE` | Primary Cadence: Ordering With Pending Work; Idle: External Goal Mutation Behavior; Ext Ownership: Required Replacement Shape | Account usage if needed, persist durable mutation, persist pending intent when mutation creates cadence work, and request same-turn cadence recheck only if the active turn can accept turn-local cadence request metadata. Same-turn recheck is metadata/wake behavior only: it must not construct active model input, choose role, or consume intent. If accepted, pending intent is still consumed only when that active turn's final request input contains the matching developer-role Goal item and reaches commit. If unavailable or rejected, intent remains pending. Pending non-Goal work still runs before Goal-owned synthetic turns. | `ext/goal/src/runtime.rs`, core Goal mutation paths, turn-local cadence request metadata | `T-IDLE`, `T-EXT`, `T-DURABLE`, `T-CADENCE`, `T-FINAL` | High-risk |
| `ext/goal` ownership | `T-EXT` | Ext Ownership: Purpose, Ownership Decision, Required Replacement Shape; Fake-Shim Map: Extension Goal Steering Producer; Open Deliverables: ext ownership | Extension may own lifecycle/tools/accounting/mutation and typed cadence request metadata through a producer-facing adapter seam. It must not own active model-input construction, role selection, pending-intent consumption, Continuation watermark, final cleanup/repair, prebuilt active-turn model input, or private cadence/finalizer implementation types. Same-turn extension behavior is metadata-only cadence recheck/request metadata. | `codex-rs/ext/goal`, `codex_thread.rs` | `T-EXT`, `T-FINAL` | High-risk |
| Extension reachability | `T-EXT` | Ext Ownership: Reachability Rule; Fake-Shim Map: Extension producer and Required Work Areas; Test Deletion Map: extension backend baseline | If compiled/reachable active extension steering remains, it must be converted to shared final request-input shaping and cadence delivery; otherwise removed or proven unreachable under every supported configuration. Cannot leave reachable `GoalContext`, user-role internal context, or pre-finalizer concrete items. | Extension tests, config/compile structure, shared finalizer conversion | `T-EXT`, `T-SHIM`, `T-TEST-PREP`, `T-FINAL`, `T-CADENCE` | High-risk, Test-critical |
| Steering-role config compatibility | `T-EXT` | Grounding Truth: Required Active Steering Shape; Ext Ownership: Configuration; Fake-Shim Map: Core Goal Steering Producer; Test Deletion Map: Revert Steering-Role Config Overlay | User-role active Goal steering must not survive as compatibility. Existing config key must be removed, rejected, or hard-mapped to developer-role behavior; temporary deserialization cannot affect active steering role. | `ConfigToml`, `config.schema.json`, config tests | `T-EXT`, `T-BEHAVIOR`, `T-TEST-PREP` | High-risk |
| Fake-shim removal | `T-SHIM` | Fake-Shim Map: entire doc; Primary Cadence: Fake-Shim Deletion Target; Grounding Truth: Anti-Patterns/Legacy Handling | `GoalContext`, `GoalContextRole`, active `<goal_context>`, active fake provenance predicates, and active construction paths are deletion terrain. Legacy artifact handling may remain but cannot keep active Goal-specific context architecture alive. | `goal_context.rs`, `goals.rs`, `ext/goal/steering.rs`, consumers | `T-SHIM`, `T-CLEANUP`, `T-FINAL` | High-risk, Cross-cutting |
| Legacy Goal artifact handling | `T-CLEANUP` | Grounding Truth: Legacy Goal Artifact Handling; Primary Cadence: Legacy Goal Artifact; Fake-Shim Map: Required Legacy Artifact Handling; Repair/Classifier: Classifier Outputs | Pure legacy `<goal_context>` may be recognized for cleanup/projection/compaction/reconstruction and preventing ordinary-prose treatment. It must not recover state, infer objective, decide cadence, create active steering, preserve user-role behavior, or migrate sessions into active Goals. | Legacy classifier/projection/compaction/reconstruction tests | `T-CLEANUP`, `T-SHIM` | High-risk, Test-critical |
| Runtime archaeology forbidden | `T-BEHAVIOR` | Grounding Truth: Durable State, Runtime Archaeology anti-pattern; Primary Cadence: Current Authority, Legacy Goal Artifact; Repair/Classifier: Reconstruction | Runtime must not parse rendered Goal artifacts to recover active Goal state, current objective, budget state, cadence intent, or pending steering kind. | Resume/reconstruction/fork/rollback tests | `T-BEHAVIOR`, `T-CLEANUP`, `T-DURABLE` | High-risk |
| Classifier outputs | `T-CLEANUP` | Repair/Classifier: Classifier Outputs; Primary Cadence: Shared Classification; Fake-Shim Map: Classifiers And Legacy Handling | Classifier must distinguish current Goal internal context, legacy Goal artifact, non-Goal internal context, and mixed/ordinary content. Structured classifications carry role, source or legacy identity, body fingerprint where applicable, and purity. Classification is cleanup support and never authority proof; current Goal classification is not authority unless final input also contains the selected outer developer-role item matching durable facts. | Classifier module/tests | `T-CLEANUP`, `T-FINAL` | High-risk, Test-critical |
| Purity rules | `T-CLEANUP` | Repair/Classifier: Purity Rules; Fake-Shim Map: Event/UI Hiding, History Boundaries | Pure item must be a whole-message `ResponseItem::Message` with user or developer role, exactly one text content item, recognized representation after trimming, and no mixed prose. Mixed ordinary content must remain visible and not be hidden/dropped/deduplicated or treated as authority. Wrong-role current Goal may be cleanup-classified but not valid authority. | Classifier/projection/history tests | `T-CLEANUP`, `T-FINAL` | High-risk, Test-critical |
| Typed/materialized projection | `T-CLEANUP` | Grounding Truth: Legacy Artifact Handling; Repair/Classifier: Event Mapping And Typed Projection; Fake-Shim Map: Event And UI Hiding | Typed/materialized projections may hide pure current Goal internal-context items and legacy artifacts. Projection hiding is not authority. Mixed prose remains visible. | `event_mapping.rs`, app-server typed/materialized history | `T-CLEANUP` | High-risk, Test-critical |
| Raw response item notifications | `T-CLEANUP` | Grounding Truth: Legacy Artifact Handling, Acceptance Standard; Primary Cadence: Legacy Goal Artifact, Verification Checklist; Repair/Classifier: Raw Response Notifications; Test Deletion Map: fake context tests | Raw notifications remain raw. Do not add Goal-specific suppression for current Goal items, legacy Goal artifacts, or mixed prose unless the general raw-response contract changes. Existing local suppression is removal terrain. | `app-server/src/bespoke_event_handling.rs`, raw tests | `T-CLEANUP`, `T-TEST-PREP` | High-risk, Test-critical |
| Compaction | `T-CLEANUP` | Grounding Truth: Request Repair, Acceptance Standard; Fake-Shim Map: Compaction; Repair/Classifier: Compaction; History Key: Compaction And Reconstruction | Compaction filters legacy artifacts and stale/duplicate current items as cleanup. It may preserve committed carry metadata for a cadence item already included in final input or apply request-local repair when cadence-required authority would otherwise be lost. It must not carry pre-finalizer concrete input, create durable facts or pending intent, create active-state-only Goal items, or treat filtering as authority. For T-HISTORY, pure Goal cleanup does not change the key, compaction summaries that replace eligible progress participate in the projection, and metadata with no model-visible eligible-progress effect does not permit another Continuation. | `compact.rs`, `compact_remote.rs`, `compact_remote_v2.rs`, history key tests | `T-CLEANUP`, `T-FINAL`, `T-HISTORY` | High-risk, Test-critical |
| Rollout reconstruction, rollback, fork | `T-CLEANUP` | Grounding Truth: Request Repair, Acceptance Standard; Fake-Shim Map: Rollout Reconstruction; Repair/Classifier: Rollout Reconstruction, Rollback, And Fork; History Key docs; Recorded Evidence: Rollback And Fork | Filter legacy artifacts, clean/dedupe current items by final repair rules, preserve mixed ordinary messages, never recover active state/intent/objective from rendered artifacts. Structured committed metadata may reconstruct a lost recorded cadence item. For T-HISTORY, reconstruction computes the same key from surviving model-visible history that live history would compute; rollback/fork use surviving durable watermark or surviving replay evidence for Continuation decisions and never old rendered Goal text. | `rollout_reconstruction.rs`, rollback/fork callers, history key tests | `T-CLEANUP`, `T-FINAL`, `T-HISTORY`, `T-EVIDENCE` | High-risk, Test-critical |
| History and user-turn boundaries | `T-CLEANUP` | Fake-Shim Map: History And User-Turn Boundaries; Repair/Classifier: Contextual Parsing And History Boundaries | Pure current/legacy Goal internal context must not become ordinary user-turn boundaries; ordinary user messages still count; rollback may trim pure contextual fragments; mixed messages remain ordinary. | `contextual_user_message.rs`, `history.rs` | `T-CLEANUP` | High-risk |
| Previous response/model-context transitions | `T-FINAL` | Grounding Truth: Request Repair, Acceptance Standard; Primary Cadence: Proving Current Authority, Request Repair | previous_response_id or model-context reuse is not proof by itself. It must inspect/account for final input to prove current authority; otherwise repair must restore cadence-required developer-role authority for that request. | previous_response_id/model-context paths | `T-FINAL`, `T-CLEANUP` | High-risk |
| Tool output and UI state | `T-BEHAVIOR` | Grounding Truth: Core Truth, Tool Output As Steering, Hiddenness As Authority; Primary Cadence: Durable Goal Facts/Proving Current Authority | Goal tool output may report Goal state but is not steering. UI state/projection/hiddenness does not prove model authority or current durable facts. | Tool/app-server/TUI surfaces | `T-BEHAVIOR`, `T-CADENCE` | Open |
| Upstream Goal product baseline | `T-TEST-PREP` | AGENTS: Non-Negotiables/Test Prep; Test Deletion Map: intro, Upstream Baseline Tests That Remain Active | App-server Goal APIs, `/goal`, status/footer projection, pause/edit/clear, budget/usage/status behavior return to upstream baseline unless a later product change explicitly replaces them. Budget and usage are upstream facts, not local experiments. Extension baseline is limited by the conversion/removal caveat: a slice that removes or converts `ext/goal` active Goal steering must update or remove old extension tests and must not let them keep `GoalContext` or `<goal_context>` alive. | Named core/app-server/TUI/ext tests | `T-TEST-PREP`, `OP-AGENTS`, `T-EXT`, `T-SHIM` | High-risk; `T-EXT`/`T-SHIM` are shared only for the extension-test caveat |
| Local overlay deletion | `T-TEST-PREP` | Test Deletion Map: Delete Local-Only Fake Context/Core/App-Server/TUI, Revert Steering-Role Config Overlay | Delete local-only tests defending broken fake context, bad cadence, user-role steering, local raw suppression, configured steering role, and local overlay behaviors. Do not delete upstream baseline tests. | Listed tests | `T-TEST-PREP` | High-risk, Test-critical |
| Replacement test profile | `T-TEST-PREP` | Grounding Truth: Acceptance Standard; Primary Cadence: Verification Checklist; Idle: Acceptance Tests; Final Input: Tests; Model History Key: Tests; Repair/Classifier: Tests; Test Deletion Map: Replacement Test Profile | Replacement tests prove final request input, durable pending intent, resume/idle lifecycle, repair/legacy artifacts, raw behavior, extension paths, and local behavior re-additions after replacement state machine exists. | Future focused tests and snapshots | `T-TEST-PREP`, `T-BEHAVIOR`, `T-CADENCE`, `T-DURABLE`, `T-FINAL`, `T-IDLE`, `T-HISTORY`, `T-EVIDENCE`, `T-CLEANUP`, `T-EXT`, `T-SHIM` | High-risk, Test-critical; `T-TEST-PREP` owns the matrix, behavior targets retain local test obligations |
| Snapshot handling | `T-TEST-PREP` | Test Deletion Map: Snapshot Handling; root AGENTS snapshot guidance | Delete snapshots only with deleted local-only owner tests, restore upstream-owned snapshots to baseline, update/add snapshots only for intentional UI changes. | TUI snapshot tests | `T-TEST-PREP` | Open |
| Design readiness vs execution plan | `T-READINESS` | AGENTS: Design Deliverables; Open Design Deliverables: Current Status, Readiness Rule; README: Pass 2 Guardrails | Ready means design input, not concrete file/function/migration/test/slice order. Current seam docs are Ready as inputs; next execution plan translates to ordered file-specific slices without reopening core architecture absent direct conflict. | n/a | `T-READINESS`, `OP-AGENTS`, `NAV-README` | Open |

## Code-Grounding Debt Review

Reviewed 2026-07-14 against current v135 terrain, `rust-v0.136.0`,
`rust-v0.139.0`, and `rust-v0.140.0`.

| Concept | Authority-doc answer | Code-grounding result | Ledger action |
| --- | --- | --- | --- |
| Recorded request evidence | The source docs answer the boundary: final model payload remains the primary live request evidence, and structured recorded request evidence from rollout/thread history is acceptable only when it represents the same logical final model request input. It is not Goal authority, cadence selection, final-input inspection, pending intent storage, or active Goal recovery. | Current v135, v136, and v139 terrain persists ordinary `RolloutItem::ResponseItem(ResponseItem)`, `Compacted`, `TurnContext`, and `EventMsg` records. v140 adds typed replay precedent through `RolloutItem::InterAgentCommunication` and moves more persistence policy responsibility toward thread-store implementations, but it still does not record Goal request commit identity. Optional rollout trace can record upstream inference requests, but it is best-effort diagnostic tracing, not normal replay state. | Design pass resolved in `goal-authority-recorded-request-evidence.md`. The resolved seam uses structured committed Goal request evidence when rollout/thread history is used as replay evidence, while durable state remains the recommended live correctness owner for pending intent and Continuation suppression. |

## High-Risk Repeated Authority Families

The following repeated clauses appear across multiple source docs and must not
be silently collapsed during successor-doc compression. "Preserve" means keep
the full source-backed contract in the canonical owner, keep local reminders
where a seam can directly violate the rule, and use pointer-only or
operational/test reminders elsewhere.

| Repeated family | Compression routing |
| --- | --- |
| Final request-input developer-role proof | Batch 1: `T-FINAL` carries proof mechanics, `T-BEHAVIOR` carries behavioral truth, local reminders stay in cadence/cleanup/extension/test prep. |
| Pending Initial, ObjectiveUpdated, and BudgetLimit until final-input commit | Batch 1: `T-DURABLE` carries pending-intent shape, `T-FINAL` carries commit timing, cadence/idle/extension/test prep keep local reminders. |
| Exact-key consumption | Batch 1: `T-DURABLE` carries exact-key store semantics, `T-FINAL` carries when consumption may be called, cadence/idle/test prep keep local reminders. |
| Active durable state alone is not steering or cadence authority | Batch 1: `T-BEHAVIOR`, `T-CADENCE`, and `T-DURABLE` carry the rule at their seams; final/cleanup/idle/extension keep local reminders. |
| Ordinary user turns are not cadence events | Batch 1: `T-CADENCE` and `T-IDLE` carry the rule; final/test prep keep local reminders. |
| Automatic Continuation and watermarking | Batch 2: `T-IDLE` owns selection, `T-HISTORY` owns watermark comparison, and `T-FINAL` owns commit-time advancement. |
| Resume hydration | Batch 2: `T-IDLE` owns lifecycle hydration, `T-DURABLE` owns reloaded facts/pending intent, and `T-HISTORY` owns suppression basis. |
| Retry, follow-up, and same-turn cadence recheck metadata | Batch 2: `T-FINAL` owns retry/follow-up shaping and commit timing; `T-IDLE` owns synthetic metadata lifecycle and same-turn wake routing; same-turn metadata is not prebuilt model input. |
| Current-turn carry | Batch 2: `T-FINAL` owns committed carry metadata; cleanup/idle/history/test prep keep local reminders that carry is not pre-finalizer concrete input. |
| Request repair is request-local, not cadence | Batch 3: `T-CLEANUP` owns repair/classifier semantics, `T-FINAL` owns repair inside final input, and `T-CADENCE` owns the negative cadence rule. |
| Classifier/provenance/helper output/projection are not authority | Batch 3: `T-CLEANUP` owns classifier/projection details, `T-BEHAVIOR` owns the negative rule, and `T-FINAL` owns the only authority seam. |
| Structured recorded request evidence | Batch 3: `T-EVIDENCE` owns replay/audit metadata, `T-FINAL` owns finalized-input identity, and durable/history/cleanup/test/readiness keep local boundary reminders. |
| Raw response item notifications remain raw | Batch 3: `T-CLEANUP` owns raw/projection distinction, evidence/test prep keep local reminders. |
| Compaction, reconstruction, rollback, and fork | Batch 3: `T-CLEANUP` owns cleanup/reconstruction behavior, `T-HISTORY` owns key effects, `T-EVIDENCE` owns committed metadata, and `T-FINAL` owns carry/repair at final input. |
| Extension reachability and steering-role config compatibility | Batch 4: `T-EXT` owns lifecycle/config/reachability, while behavior/cadence/durable/final/idle/shim/test prep keep local reminders. |
| Fake-shim removal | Batch 4: `T-SHIM` owns demolition terrain; cleanup/final/extension/test prep own replacement-seam details. |
| Replacement testing and upstream baseline | Batch 4: `T-TEST-PREP` owns prep sequence and matrix, while behavior/seam targets keep local proof obligations. |
| Navigation, glossary, agents, and readiness surfaces | Batch 4: `NAV-README`, `GLOSSARY`, `OP-AGENTS`, and `T-READINESS` stay navigation/vocabulary/operational/handoff surfaces, not behavior engines. |

## Repeated Authority Canonicalization Alignment

Use `SUCCESSOR_DOC_COMPRESSION_GUIDE.md` as repeated-authority compression
guidance. Do not infer from this ledger that every repeated sentence should
survive in every successor doc.

During future concept-preserving rewrite planning:

- write canonical text in the owning target before replacing repeated source
  prose elsewhere
- keep a local reminder where the target seam can directly violate the rule
- use pointer-only references where another target owns the rule and the local
  seam cannot directly violate it
- keep operational/test reminders short and explicitly non-authoritative
- keep source coverage checks when a named disputed concept, suspected loss, or
  conflict requires them

## Pass 2A Notes

- This ledger intentionally records concepts at a level above individual
  sentences. Future rewrite planning still needs source coverage checks.
- `High-risk` means the concept should receive explicit coverage notes before
  the relevant content is rewritten.
- No concept is closed by this file.
